import {input, print} from './\io_utils';

function main(){

        const a = Number(input('Entre com o numero a'));
        const b = Number(input('Entre com o numero a'));
        
        const X = a + b;
        
        print(`X = ${X}`);
}

main()